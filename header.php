<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Level Up</title>

    <!--Reglas de estilo de Bootstrap 5-->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.3/font/bootstrap-icons.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="css/style.css"/>
    <!--<link rel="stylesheet" href="css/juegoEstilos.css"/>
    <link rel="stylesheet" href="css/perfilusuarioEstilos.css"/>
    <link rel="stylesheet" href="css/registroEstilos.css"/>-->

    
    <!--Reglas de estilo del proyecto-->
    <!--<style>
      body{
        background-color: #181818;
      }

      .text-justify{
        text-align: justify;
      }
      /*Reglas de estilo para el carrusel de inicio*/
      .carousel{
        height: 80vh;
      }

      .carousel-item{
        height:80vh;
        background:#000;
        color:white;
        position: relative;
      }

      .carousel-heading{
        font-size: 3.5rem;
      }

      .carousel-text{
        font-size: 1.5rem;
      }

      .overlay-image{
        position: absolute;
        bottom:0;
        left:0;
        right:0;
        top:0;
        background-position: center;
        background-size:cover;
        opacity: 0.4;
      }

      /*Reglas de estilo para el banner de inicio*/

      .contenedorBanner{
        width: 60%;
        max-width: 1200 px;
        margin: auto;
        padding: 40px 0;

        display:grid;
        grid-template-columns: 1fr;
        gap:20px;
      }

      .contenedorImagenBanner{
        display: grid;
        grid-template-columns: 1fr;
        max-height: 100 px;
        gap:20px;
      }

      /*Reglas de estilo para la sección de vídeos de inicio*/
      .contenedor{
        width: 90%;
        max-width: 1200 px;
        margin: auto;
        padding: 40px 0;

        display:grid;
        grid-template-columns: 4fr;
        gap:20px;
      }

      .contenedorVideos{
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap:20px;
      }

      .card{
        border-radius: 10px;
        min-height: 200 px;
        font-weight: bold;
        padding: 20px;
        position: relative;
        overflow: hidden;
        background-size: cover;
        background-position: center center;
      }


      /*Reglas de estilo de registrarse*/
      /*Reglas de estilo de iniciar sesión*/

      /*Reglas de estilo de tienda*/
      .contenedorTarjetas{
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap:20px;
      }

      .gradient-custom {
        /* fallback for old browsers */
        background: #111111;
    
        /* Chrome 10-25, Safari 5.1-6 */
        background: -webkit-linear-gradient(to right, rgb(236, 232, 3), rgba(5, 5, 5, 0.733));
    
        /* W3C, IE 10+/ Edge, Firefox 16+, Chrome 26+, Opera 12+, Safari 7+ */
        background: linear-gradient(to right, rgba(236, 232, 3, 0.555), rgba(5, 5, 5, 0.733))
      }

      .card-header-gold{
        height: 25px;
        background: linear-gradient(-98deg, #8f8f8f, #ffc107);
      }

      .contenedorFotos{
        width: 90%;
        max-width: 1200 px;
        margin: auto;
        padding: 40px 0;

        display:grid;
        grid-template-columns: 1fr;
        gap:20px;
      }

      .fotos{
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        max-height: 100 px;
        gap:20px;
      }

    </style> -->
  </head>
  <body>